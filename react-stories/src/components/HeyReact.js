import React from 'react';
import PropTypes from 'prop-types';

const HeyReact = props => {
  return <h1 style={{ textAlign: 'center' }}>Hey React</h1>;
};

HeyReact.propTypes = {};

export default HeyReact;
